# gps-tracker-firmware
Production ESP32 asset tracker firmware. GPS acquisition with durable SD buffering and ACK-gated MQTT delivery over Wi-Fi or cellular — built so that losing connectivity never means losing data.
